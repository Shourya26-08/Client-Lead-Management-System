import {useState} from 'react';import Modal from './Modal';
export default function LeadForm({lead,onClose,onSave}){const [form,setForm]=useState(lead||{name:'',email:'',phone:'',source:'Website',status:'new',followUpAt:''});const set=(k,v)=>setForm({...form,[k]:v});return <Modal title={lead?'Edit lead':'Add new lead'} onClose={onClose}><form onSubmit={e=>{e.preventDefault();onSave(form)}}><div className="form-grid"><label>Name<input required value={form.name} onChange={e=>set('name',e.target.value)}/></label><label>Email<input required type="email" value={form.email} onChange={e=>set('email',e.target.value)}/></label><label>Phone<input value={form.phone||''} onChange={e=>set('phone',e.target.value)}/></label><label>Source<select value={form.source} onChange={e=>set('source',e.target.value)}><option>Website</option><option>LinkedIn</option><option>Referral</option><option>Google</option><option>Other</option></select></label><label>Status<select value={form.status} onChange={e=>set('status',e.target.value)}><option value="new">New</option><option value="contacted">Contacted</option><option value="converted">Converted</option></select></label><label>Follow-up<input type="datetime-local" value={form.followUpAt?new Date(form.followUpAt).toISOString().slice(0,16):''} onChange={e=>set('followUpAt',e.target.value)}/></label></div><div className="actions"><button type="button" className="secondary" onClick={onClose}>Cancel</button><button className="primary">Save lead</button></div></form></Modal>}
