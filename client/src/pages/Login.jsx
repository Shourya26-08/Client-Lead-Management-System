import {useState} from 'react';import {useAuth} from '../context/AuthContext';
export default function Login(){const {login}=useAuth();const [email,setEmail]=useState('admin@example.com'),[password,setPassword]=useState(''),[error,setError]=useState('');return <main className="login"><div className="login-card"><div className="logo">CL</div><h1>Client Lead CRM</h1><p>Secure admin workspace</p><form onSubmit={async e=>{e.preventDefault();setError('');try{await login({email,password})}catch(x){setError(x.message)}}}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter admin password"/></label>{error&&<div className="error">{error}</div>}<button className="primary full">Sign in</button></form></div></main>}
