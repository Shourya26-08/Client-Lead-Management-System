const BASE=import.meta.env.VITE_API_URL||'http://localhost:5000/api';
async function request(path,options={}){const token=localStorage.getItem('crm_token');const res=await fetch(`${BASE}${path}`,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...(options.headers||{})}});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.message||'Request failed');return data;}
export const api={login:(body)=>request('/auth/login',{method:'POST',body:JSON.stringify(body)}),stats:()=>request('/leads/stats'),leads:(params='')=>request(`/leads${params}`),create:(body)=>request('/leads',{method:'POST',body:JSON.stringify(body)}),update:(id,body)=>request(`/leads/${id}`,{method:'PATCH',body:JSON.stringify(body)}),remove:(id)=>request(`/leads/${id}`,{method:'DELETE'}),note:(id,text)=>request(`/leads/${id}/notes`,{method:'POST',body:JSON.stringify({text})})};
